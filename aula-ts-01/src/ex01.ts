//! Problemas que o TS consegue resolver:
//!   - 1: Tipagem Forte
//!   - 2:

//? Duvidas:
//? apareceu esse erro
//? SyntaxError: Unexpected token ':'


function somar(num1: number, num2: number) {
  return num1 + num2;
}

const resultado = somar(1, 3);
console.log(resultado);
